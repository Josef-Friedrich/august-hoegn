baseurl:
encoding: utf-8

collections:
  compositions:
    output: true

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: default

exclude:
- "*.php"
- README.md
